import React from 'react'
import styled from 'styled-components'

const Header = styled.header`
  background: #555;
`

export default () => {
  return <Header>Simply Window St - Christopher Muller</Header>
}
